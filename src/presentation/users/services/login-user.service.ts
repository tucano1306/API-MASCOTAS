export class LoginUserService {
    constructor() {}
  
    async execute(credentials: { email: string; password: string }) {
      // Por ahora, según las instrucciones, este método no está implementado
      return { message: 'not yet implemented' };
    }
  }